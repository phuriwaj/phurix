---
/**
 * Home — `/` (DESIGN.md §4 — identity page).
 * Hero (HeroSection from hero-section-5 — React island) +
 * curated picks + four recent strips (Essays / Notes / Patterns /
 * Library) + dark-near-black Newsletter closing band.
 * Cite: DESIGN.md §4 (home).
 */
import Layout from '@/layouts/Layout.astro';
import { HeroSection } from '@/components/blocks/hero-section-5';
import GardenCard from '@/components/GardenCard.astro';
import CollectionGrid from '@/components/CollectionGrid.astro';
import { Button } from '@/components/ui/button';
import CuratedPicks from '@/components/CuratedPicks.astro';
import NewsletterBand from '@/components/NewsletterBand.astro';
import BookTile from '@/components/BookTile.astro';
import { getCollection } from 'astro:content';

const allEssays = await getCollection('essays');
const allNotes = await getCollection('notes');
const allPatterns = await getCollection('patterns');
const allBooks = await getCollection('books');

const recentEssays = allEssays
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime())
  .slice(0, 3);

const recentNotes = allNotes
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime())
  .slice(0, 3);

const recentPatterns = allPatterns
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime())
  .slice(0, 3);

const recentBooks = allBooks
  .sort((a, b) => (b.data.year ?? 0) - (a.data.year ?? 0))
  .slice(0, 6);

const curatedPicks = [
  { title: 'On Typography, and the Trust It Quietly Asks For', href: '/essays/on-typography-and-trust' },
  { title: 'Design Systems Are Grammar, Not Paint', href: '/essays/design-systems-are-grammar' },
  { title: 'Token-as-Grammar', href: '/patterns/token-as-grammar' },
];

const validSlugs = new Set([
  ...allEssays.map((e) => `essays/${e.id}`),
  ...allNotes.map((n) => `notes/${n.id}`),
  ...allPatterns.map((p) => `patterns/${p.id}`),
]);
for (const pick of curatedPicks) {
  const path = pick.href.replace(/^\//, '');
  if (!validSlugs.has(path)) {
    console.warn(`[curated-picks] broken link: "${pick.title}" → ${pick.href}`);
  }
}
---

<Layout title="phurix" description="Personal website of Phuriwaj Ruengnaowaroj">
  <HeroSection client:load />

  <section class="container-phx section border-t border-hairline">
    <CuratedPicks items={curatedPicks} />
  </section>

  <section class="container-phx section border-t border-hairline">
    <div class="flex items-baseline justify-between gap-5 mb-8">
      <h2>Essays</h2>
      <Button asChild variant="outline" className="rounded-full h-auto px-6 py-3 text-base">
        <a href="/essays">All essays →</a>
      </Button>
    </div>
    <CollectionGrid cols={3} gap="md">
      {recentEssays.map((essay) => (
        <GardenCard
          title={essay.data.title}
          type="essay"
          lede={essay.data.lede}
          date={essay.data.date}
          topics={essay.data.topics}
          href={`/essays/${essay.id}`}
        />
      ))}
    </CollectionGrid>
  </section>

  <section class="container-phx section border-t border-hairline">
    <div class="flex items-baseline justify-between gap-5 mb-8">
      <h2>Notes</h2>
      <Button asChild variant="outline" className="rounded-full h-auto px-6 py-3 text-base">
        <a href="/notes">All notes →</a>
      </Button>
    </div>
    <CollectionGrid cols={3} gap="md">
      {recentNotes.map((note) => (
        <GardenCard
          title={note.data.title}
          type="note"
          excerpt={note.data.excerpt}
          date={note.data.date}
          topics={note.data.topics}
          growthStage={note.data.growthStage}
          href={`/notes/${note.id}`}
        />
      ))}
    </CollectionGrid>
  </section>

  <section class="container-phx section border-t border-hairline">
    <div class="flex items-baseline justify-between gap-5 mb-8">
      <h2>Patterns</h2>
      <Button asChild variant="outline" className="rounded-full h-auto px-6 py-3 text-base">
        <a href="/patterns">All patterns →</a>
      </Button>
    </div>
    <CollectionGrid cols={3} gap="md">
      {recentPatterns.map((pattern) => (
        <GardenCard
          title={pattern.data.title}
          type="pattern"
          excerpt={pattern.data.lede}
          date={pattern.data.date}
          topics={pattern.data.topics}
          href={`/patterns/${pattern.id}`}
        />
      ))}
    </CollectionGrid>
  </section>

  <section class="container-phx section border-t border-hairline">
    <div class="flex items-baseline justify-between gap-5 mb-8">
      <h2>Library</h2>
      <Button asChild variant="outline" className="rounded-full h-auto px-6 py-3 text-base">
        <a href="/library">Browse the library →</a>
      </Button>
    </div>
    <div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-6 gap-7 lg:gap-5">
      {recentBooks.map((book) => (
        <BookTile
          title={book.data.title}
          author={book.data.author}
          coverColor={book.data.coverColor}
          year={book.data.year}
        />
      ))}
    </div>
  </section>

  <section class="bg-near-black text-on-dark py-15 mt-15">
    <div class="container-phx">
      <NewsletterBand />
    </div>
  </section>
</Layout>
